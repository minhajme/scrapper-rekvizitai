{
  "name": "scraper-rekvizitai-redis-cache-adapter",
  "version": "1.0.0",
  "description": "redis cache adapter for the project scraper rekvizitai. this is born due to the redis support of php being very poor, so I did the redis operations from js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Minhajul Anwar",
  "license": "MIT",
  "dependencies": {
    "@redis/search": "^1.1.3",
    "amqplib": "^0.10.3",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "nodemon": "^3.0.1",
    "redis": "^4.6.7"
  }
}
